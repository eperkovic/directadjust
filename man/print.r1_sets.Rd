% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/r1_combine.R
\name{print.r1_sets}
\alias{print.r1_sets}
\title{Print certified adjustment sets}
\usage{
\method{print}{r1_sets}(x, ...)
}
\arguments{
\item{x}{An \code{r1_sets} object, as returned by \code{\link[=r1_entner]{r1_entner()}}, \code{\link[=r1_build]{r1_build()}},
or \code{\link[=r1_combine]{r1_combine()}}.}

\item{...}{Ignored, present for consistency with the generic.}
}
\value{
\code{x}, invisibly.
}
\description{
Prints the procedure that produced the object, the treatments and outcome
it applies to, the number of conditional independence tests performed, and
each certified set. The empty set prints as \code{{ }}.
}
\examples{
set.seed(1)
n <- 4000
C  <- rnorm(n); W1 <- rnorm(n); W2 <- rnorm(n)
X1 <- C + W1 + rnorm(n)
X2 <- C + W2 + rnorm(n)
Y  <- X1 + X2 + C + rnorm(n)
print(r1_build(data.frame(C, W1, W2, X1, X2, Y), "X1", "X2", "Y"))

}
