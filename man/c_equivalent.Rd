% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/c_equivalent.R
\name{c_equivalent}
\alias{c_equivalent}
\title{Test c-equivalence of two candidate adjustment sets}
\usage{
c_equivalent(
  data,
  x,
  y,
  z,
  t,
  alpha = 0.05,
  alpha_indep = alpha,
  tester = NULL
)
}
\arguments{
\item{data}{A numeric data frame or matrix.}

\item{x}{Treatment column name(s).}

\item{y}{Column name of the outcome.}

\item{z, t}{The two candidate sets (character vectors; either may be
\code{character(0)} for the empty set).}

\item{alpha}{Significance level for declaring dependence (default 0.05,
as in the paper's simulations). Ignored when \code{tester} is supplied.}

\item{alpha_indep}{Threshold for declaring independence (p-value above
it). Defaults to \code{alpha}; setting it higher (e.g. 0.1) certifies a set
only on decisive evidence, as in Entner, Hoyer and Spirtes (2013).
Ignored when \code{tester} is supplied.}

\item{tester}{Optional tester from \code{\link[=make_ci_tester]{make_ci_tester()}}, e.g. to share a
test counter across procedures.}
}
\value{
A list with \code{equivalent} (logical), \code{criterion} (\code{"i"}, \code{"ii"},
or \code{NA} when neither holds), and \code{n_tests}.
}
\description{
Implements the probabilistic criteria for confounding equivalence (the
paper's Theorem 3, after Pearl and Paz): sets \eqn{Z} and \eqn{T} are
declared c-equivalent relative to \eqn{(X, Y)} when either
\describe{
\item{(i)}{\eqn{X \perp Z \mid T} and \eqn{Y \perp T \mid Z \cup X}, or}
\item{(ii)}{\eqn{X \perp T \mid Z} and \eqn{Y \perp Z \mid T \cup X}.}
}
If \eqn{T} is c-equivalent to a certified adjustment set (for example one
found by \code{\link[=r1_entner]{r1_entner()}} or \code{\link[=r1_combine]{r1_combine()}}), then \eqn{T} is an adjustment
set as well, which extends the reach of the data-driven rules; see the
paper's Examples 2, 4, and 9.
}
\details{
Joint independence statements involving sets are decided through the
chain decomposition: \eqn{A \perp B \mid C} holds when every
\eqn{A_i \perp B_j \mid C \cup A_{<i} \cup B_{<j}} is declared
independent, each by a Fisher-z test.
}
\examples{
# Z is an adjustment set; the empty set is c-equivalent to it when
# X and Z are marginally independent (criterion i)
set.seed(1)
n <- 4000
Z <- rnorm(n)
X <- rnorm(n)
Y <- X + Z + rnorm(n)
d <- data.frame(Z, X, Y)
c_equivalent(d, "X", "Y", z = "Z", t = character(0))

}
\references{
Pearl, J., and Paz, A. (2014). Confounding equivalence in
causal inference. \emph{Journal of Causal Inference} 2(1):75--93.
}
