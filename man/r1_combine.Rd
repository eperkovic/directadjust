% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/r1_combine.R
\name{r1_combine}
\alias{r1_combine}
\title{R1 Combine: assemble adjustment sets from single-treatment certificates}
\usage{
r1_combine(
  data,
  x1,
  x2,
  y,
  covariates = setdiff(colnames(as.data.frame(data)), c(x1, x2, y)),
  alpha = 0.05,
  alpha_indep = alpha,
  max_size = NULL,
  tester = NULL,
  prune = c("lemma8", "witness")
)
}
\arguments{
\item{data}{A numeric data frame or matrix.}

\item{x1, x2}{Column names of the two treatments, \code{x1} causally first.}

\item{y}{Column name of the outcome.}

\item{covariates}{Character vector of candidate (pretreatment) covariate
names. Defaults to all remaining columns.}

\item{alpha}{Significance level for declaring dependence (default 0.05,
as in the paper's simulations). Ignored when \code{tester} is supplied.}

\item{alpha_indep}{Threshold for declaring independence (p-value above
it). Defaults to \code{alpha}; setting it higher (e.g. 0.1) certifies a set
only on decisive evidence, as in Entner, Hoyer and Spirtes (2013).
Ignored when \code{tester} is supplied.}

\item{max_size}{Largest size of the per-treatment candidate set \eqn{T}.
Defaults to all available candidates.}

\item{tester}{Optional tester from \code{\link[=make_ci_tester]{make_ci_tester()}}, e.g. to share a
test counter across procedures.}

\item{prune}{How each certified \eqn{T} is reduced to a minimal
adjustment set: \code{"lemma8"} (default) tests minimality through the
paper's Lemma 8; \code{"witness"} uses a faster heuristic (smallest
subset still passing the witness conditions).}
}
\value{
An object of class \code{r1_sets}; see \code{\link[=r1_build]{r1_build()}}.
}
\description{
Implements the Combine procedure (Theorem 7 of the paper). For each
treatment \eqn{X_i} separately, the procedure collects every pair of a
witness covariate \eqn{W_a} and a set \eqn{T} with
\deqn{W_a \not\perp Y \mid T \quad\text{and}\quad
      W_a \perp Y \mid T \cup \{X_i\},}
reduces the passing \eqn{T}'s to the inclusion-minimal ones, prunes each
to a minimal adjustment set \eqn{Z_i \subseteq T} (see \code{prune} below),
and returns all unions \eqn{Z_1 \cup Z_2} over pairs of pruned
per-treatment sets, with treatments removed. The candidate pool for the
second treatment includes the first treatment as a possible member of
\eqn{T} (never as a witness), reflecting that \code{x1} is a non-descendant
of \code{x2}.
}
\details{
Theorem 7 requires each \eqn{Z_i} to be a \emph{minimal} adjustment set
relative to \eqn{(X_i, Y)}; the paper's Example 12 shows that combining
non-minimal sets can produce an invalid union. The default
\code{prune = "lemma8"} therefore applies the paper's Lemma 8: \eqn{T} is
accepted as minimal when for every \eqn{T_j \in T} both
\eqn{X_i \not\perp T_j \mid T \setminus \{T_j\}} and
\eqn{Y \not\perp T_j \mid [T \setminus \{T_j\}] \cup \{X_i\}} are
declared dependent; otherwise the smallest subset \eqn{Z \subset T}
satisfying Lemma 8's conditions (iii)-(v) is used, and the candidate is
dropped when no such subset is certified. The alternative
\code{prune = "witness"} prunes \eqn{T} to the smallest subset still passing
the two witness conditions; this is cheaper but does not verify
minimality, so on structures like the paper's Example 12 it can certify
an invalid set.

As with \code{\link[=r1_build]{r1_build()}}, covariates must be pretreatment and \code{x1} must
precede \code{x2} causally; these are assumptions, not tested.
}
\note{
The paper's Theorem 7 covers any number of treatments; this
implementation specializes to two, matching the simulation study. The
general case pares and unions one certified set per treatment in the
same way.
}
\examples{
set.seed(1)
n <- 4000
C  <- rnorm(n); W1 <- rnorm(n); W2 <- rnorm(n)
X1 <- C + W1 + rnorm(n)
X2 <- C + W2 + rnorm(n)
Y  <- X1 + X2 + C + rnorm(n)
d  <- data.frame(C, W1, W2, X1, X2, Y)
r1_combine(d, "X1", "X2", "Y")

}
