% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/r1_build.R
\name{r1_build}
\alias{r1_build}
\title{R1 Build: certify adjustment sets for two treatments in one pass}
\usage{
r1_build(
  data,
  x1,
  x2,
  y,
  covariates = setdiff(colnames(as.data.frame(data)), c(x1, x2, y)),
  alpha = 0.05,
  alpha_indep = alpha,
  max_size = NULL,
  tester = NULL
)
}
\arguments{
\item{data}{A numeric data frame or matrix.}

\item{x1, x2}{Column names of the two treatments, \code{x1} causally first.}

\item{y}{Column name of the outcome.}

\item{covariates}{Character vector of candidate (pretreatment) covariate
names. Defaults to all remaining columns.}

\item{alpha}{Significance level for declaring dependence (default 0.05,
as in the paper's simulations). Ignored when \code{tester} is supplied.}

\item{alpha_indep}{Threshold for declaring independence (p-value above
it). Defaults to \code{alpha}; setting it higher (e.g. 0.1) certifies a set
only on decisive evidence, as in Entner, Hoyer and Spirtes (2013).
Ignored when \code{tester} is supplied.}

\item{max_size}{Largest size of the candidate set \eqn{Z} (drawn from the
covariates excluding the two witnesses). Defaults to all of them.}

\item{tester}{Optional tester from \code{\link[=make_ci_tester]{make_ci_tester()}}, e.g. to share a
test counter across procedures.}
}
\value{
An object of class \code{r1_sets}: a list with the certified \code{sets}
(list of character vectors, possibly empty), \code{found} (logical),
\code{n_tests} performed, and the query (\code{x}, \code{y}).
}
\description{
Implements the Build procedure (Theorem 5 of the paper) for two treatments
\code{x1} and \code{x2} with \code{x1} preceding \code{x2} in the causal order. The procedure
searches over ordered pairs of witness covariates \eqn{(W_a, W_b)} and
candidate sets \eqn{Z} drawn from the remaining covariates, and certifies
\eqn{Z} as a valid adjustment set for the joint effect of
\eqn{(X_1, X_2)} on \eqn{Y} when
\deqn{W_a \not\perp Y \mid Z, \quad W_a \perp Y \mid Z \cup \{X_1\},}
\deqn{W_b \not\perp Y \mid Z \cup \{X_1\}, \quad
      W_b \perp Y \mid Z \cup \{X_1, X_2\},}
with dependence and independence judged by the tester at its level. All
passing sets are returned (no early stopping), matching the paper's
simulation protocol.
}
\details{
The covariates must be known not to be descendants of the treatments
(pretreatment covariates), and \code{x1} must precede \code{x2} causally; both are
assumptions of Theorem 5, not testable by the procedure.
}
\note{
The paper's Theorem 5 covers any number of treatments; this
implementation specializes to two, matching the simulation study. The
general case follows the same pattern, chaining one witness condition
pair per treatment along the causal ordering.
}
\examples{
# C confounds both treatments and the outcome; W1, W2 are witnesses
set.seed(1)
n <- 4000
C  <- rnorm(n); W1 <- rnorm(n); W2 <- rnorm(n)
X1 <- C + W1 + rnorm(n)
X2 <- C + W2 + rnorm(n)
Y  <- X1 + X2 + C + rnorm(n)
d  <- data.frame(C, W1, W2, X1, X2, Y)
r1_build(d, "X1", "X2", "Y")   # certifies sets containing C

}
\references{
LaPlante, S., Triantafillou, S., and Perkovic, E.
Data-driven adjustment for multiple treatments. \emph{Journal of Causal
Inference} (to appear). Entner, D., Hoyer, P., and Spirtes, P. (2013).
Data-driven covariate selection for nonparametric estimation of causal
effects. \emph{AISTATS}.
}
\seealso{
\code{\link[=r1_combine]{r1_combine()}} for the per-treatment variant, and
\code{\link[=estimate_joint_effect]{estimate_joint_effect()}} to estimate the effect over certified sets.
}
