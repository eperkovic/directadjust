% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tester.R
\name{make_ci_tester}
\alias{make_ci_tester}
\title{Conditional independence tester with a test counter}
\usage{
make_ci_tester(data, alpha = 0.05, alpha_indep = alpha, test = NULL)
}
\arguments{
\item{data}{A numeric data frame or matrix; columns are variables.}

\item{alpha}{Significance level for declaring dependence: dependence is
declared when the test's p-value is at most \code{alpha}. The paper's
simulations use \code{alpha = 0.05}.}

\item{alpha_indep}{Threshold for declaring independence: independence is
declared when the p-value exceeds \code{alpha_indep}. Defaults to \code{alpha},
in which case every test is decisive. Setting \code{alpha_indep} larger
than \code{alpha} (for example 0.1, as in Entner, Hoyer and Spirtes, 2013)
leaves an undecided region in between, so a candidate set is certified
only when every required condition is decisively in the right
direction.}

\item{test}{Optional p-value function \verb{function(i, j, S, data)}: two
column names, a character vector of conditioning names, and the data;
must return the p-value of the conditional independence test of \code{i}
and \code{j} given \code{S}. Defaults to the Gaussian Fisher-z test
(\code{\link[pcalg:gaussCItest]{pcalg::gaussCItest()}}). Supply, for example, a G-square test for
discrete data or a kernel-based test for nonlinear continuous data.}
}
\value{
A list with components
\describe{
\item{\code{dep(i, j, S)}}{function of two variable names and a character
vector of conditioning names, returning \code{TRUE} when dependence is
declared (p-value at most \code{alpha});}
\item{\code{indep(i, j, S)}}{likewise, \code{TRUE} when independence is
declared (p-value above \code{alpha_indep});}
\item{\code{count()}}{function returning the number of tests performed so
far.}
}
}
\description{
Wraps a conditional independence test around a data set, keeping a
running count of the tests performed. The returned object is what
\code{\link[=r1_entner]{r1_entner()}}, \code{\link[=r1_build]{r1_build()}}, \code{\link[=r1_combine]{r1_combine()}}, and \code{\link[=c_equivalent]{c_equivalent()}}
consume, so a single tester can be shared between procedures to obtain a
combined test count.
}
\details{
The certification procedures are agnostic to the test: they consume only
the dependence/independence decisions, so any conditional independence
test appropriate for the data can be supplied through \code{test}. The
Gaussian Fisher-z default (and the linear-regression estimator in
\code{\link[=estimate_joint_effect]{estimate_joint_effect()}}) are where Gaussian/linear assumptions enter.
}
\examples{
d <- data.frame(a = rnorm(200))
d$b <- d$a + rnorm(200)
ct <- make_ci_tester(d)
ct$indep("a", "b", character(0))   # FALSE, they are dependent
ct$dep("a", "b", character(0))     # TRUE
ct$count()                          # 2

}
