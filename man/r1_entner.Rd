% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/r1_entner.R
\name{r1_entner}
\alias{r1_entner}
\title{R1 Entner: certify adjustment sets for a single treatment}
\usage{
r1_entner(
  data,
  x,
  y,
  covariates = setdiff(colnames(as.data.frame(data)), c(x, y)),
  alpha = 0.05,
  alpha_indep = alpha,
  max_size = NULL,
  tester = NULL
)
}
\arguments{
\item{data}{A numeric data frame or matrix.}

\item{x}{Treatment column name.}

\item{y}{Column name of the outcome.}

\item{covariates}{Character vector of candidate (pretreatment) covariate
names. Defaults to all remaining columns.}

\item{alpha}{Significance level for declaring dependence (default 0.05,
as in the paper's simulations). Ignored when \code{tester} is supplied.}

\item{alpha_indep}{Threshold for declaring independence (p-value above
it). Defaults to \code{alpha}; setting it higher (e.g. 0.1) certifies a set
only on decisive evidence, as in Entner, Hoyer and Spirtes (2013).
Ignored when \code{tester} is supplied.}

\item{max_size}{Largest size of the candidate set \eqn{Z} (drawn from the
covariates excluding the witness). Defaults to all of them.}

\item{tester}{Optional tester from \code{\link[=make_ci_tester]{make_ci_tester()}}, e.g. to share a
test counter across procedures.}
}
\value{
An object of class \code{r1_sets}; see \code{\link[=r1_build]{r1_build()}}.
}
\description{
Implements the first rule of Entner, Hoyer and Spirtes (2013), the
paper's Theorem 1, for a single treatment: a candidate set \eqn{Z} is
certified as an adjustment set relative to \eqn{(X, Y)} when some witness
covariate \eqn{W_a} satisfies \eqn{W_a \not\perp Y \mid Z} and
\eqn{W_a \perp Y \mid Z \cup \{X\}}. All certified sets are returned.
}
\details{
The covariates must be known not to be descendants of the treatment (the
pretreatment assumption); \eqn{X} must precede \eqn{Y}.
}
\examples{
# C confounds treatment and outcome; W is a witness
set.seed(1)
n <- 4000
C <- rnorm(n); W <- rnorm(n)
X <- C + W + rnorm(n)
Y <- X + C + rnorm(n)
d <- data.frame(C, W, X, Y)
r1_entner(d, "X", "Y")   # certifies sets containing C

}
\references{
Entner, D., Hoyer, P., and Spirtes, P. (2013). Data-driven
covariate selection for nonparametric estimation of causal effects.
\emph{AISTATS}.
}
\seealso{
\code{\link[=r1_build]{r1_build()}} and \code{\link[=r1_combine]{r1_combine()}} for multiple treatments, and
\code{\link[=c_equivalent]{c_equivalent()}} for extending the certified sets through
c-equivalence (the paper's Theorem 3).
}
